---
title: Noise
description: A little grain hides gradient banding and makes flat color feel like a surface.
section: Color
order: 2
publishedAt: "2026-07-15"
resources:
  - url: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/feTurbulence
    title: feTurbulence
    description: The SVG filter primitive that generates the noise. Everything here is built on it.
  - url: https://css-tricks.com/grainy-gradients/
    title: Grainy Gradients
    description: A thorough walkthrough of noise on gradients, with the color matrix tricks explained.
  - url: https://developer.mozilla.org/en-US/docs/Web/CSS/mix-blend-mode
    title: mix-blend-mode
    description: How the grain layer combines with the color underneath it.
---

When the UI feels too flat, sometimes I like to add noise to some components to give them some texture.

**Noise** is a layer of random light and dark pixels laid over the surface. It breaks the steps up and gives the color some texture. Drag the slider to add grain to this card.

<NoiseDemo />

## Grain size

The `baseFrequency` attribute controls how fine the noise is. Low values give big soft blobs, whle high values give the tight speckle of film grain.

<NoiseFrequencyDemo />

## Usage

Put the filter in one SVG anywhere on the page, then reference it from an overlay. The overlay is an empty element, so it costs nothing in markup and can be dropped onto any positioned container.

<CodeBlock
  tabs={[
    {
      label: "Tailwind",
      language: "html",
      icon: "tailwind",
      code: `<svg class="absolute size-0" aria-hidden="true">
  <filter id="grain">
    <feTurbulence type="fractalNoise" baseFrequency="0.8" numOctaves="3" stitchTiles="stitch" />
    <feColorMatrix type="saturate" values="0" />
  </filter>
</svg>

<div class="relative isolate overflow-hidden rounded-2xl bg-violet-600">
  <div class="pointer-events-none absolute inset-0 opacity-[0.08] mix-blend-overlay filter-[url(#grain)]" aria-hidden="true"></div>
  <!-- content -->
</div>`,
    },
    {
      label: "CSS",
      language: "css",
      icon: "css",
      code: `.surface {
  position: relative;
  isolation: isolate;
  overflow: hidden;
}

.surface::after {
content: "";
position: absolute;
inset: 0;
filter: url(#grain);
opacity: 0.08;
mix-blend-mode: overlay;
pointer-events: none;
}`,
},
]}
/>

The `isolate` on the container matters. Without it, `mix-blend-mode` blends the grain with everything behind the card, including the page background, and the effect changes depending on where the card sits. With it, the grain only ever blends with the surface it belongs to.
